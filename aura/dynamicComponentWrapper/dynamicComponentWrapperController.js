({
    doInit: function (component, event, helper) {
        helper.init(component);
    },

    handleMessageSubscription: function (component, event, helper) {
        helper.handleMessageSubscription(component, event);
    }
});